class Main {
    public static void main(String[] args) {
        // Block 1:
        char[] s = {'x', 'y', 'z'};
        // Block 2:
        System.out.println("" + s[0] + (s.length - 2) + s[s.length - 1]);
        // Block END.
    }
}
