class Main {
    public static void main(String[] args) {
        // Block 1:
        int a = 20, b = 15;
        // Block 2:
        System.out.println((a - (b * 2)) > 0 ? (a - (b * 2)) : 0);
        // Block END.
    }
}
