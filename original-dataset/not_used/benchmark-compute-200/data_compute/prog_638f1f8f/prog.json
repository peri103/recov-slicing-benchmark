{
  "Code": "import java.util.LinkedHashSet;\n\npublic class Main {\n    public static void main(String[] args) {\n        LinkedHashSet<Integer> set = new LinkedHashSet<>();\n\n        // Some initial operations\n        set.add(10);\n        set.add(20);\n\n        // Misleading operation\n        for (int i = 0; i < 5; i++) {\n            set.add(i);\n        }\n\n        // Conditional logic\n        boolean condition = (Math.random() > 0.5);\n\n        // Complex obfuscation using intermediate variables\n        int temp = 0;\n        if (condition) {\n            temp = 5;\n        } else {\n            temp = 10;\n        }\n\n        // Unrelated manipulation\n        set.add(30);\n\n        // The specific write point\n        if (temp + 10 > 15) {\n            /* write */ set.add(25);\n        }\n\n        // Continue operations that do not affect the read point\n        int sum = 0;\n        for (Integer num : set) {\n            sum += num;\n        }\n\n        // A new addition that doesn't affect the read point\n        set.add(40);\n\n        // The read point\n        int index = 0;\n        Integer[] array = set.toArray(new Integer[0]);\n        /* read */ int ret = array[2];\n        \n        // Output the result\n        System.out.println(\"Read value: \" + ret);\n    }\n}",
  "Internal Variable": "LinkedHashSet: element at position 2"
}