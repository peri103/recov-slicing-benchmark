{
    "Code": "import java.util.List;\nimport java.util.concurrent.CopyOnWriteArrayList;\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Integer> list = new CopyOnWriteArrayList<>();\n        // Initial population\n        list.add(10);\n        list.add(20);\n        list.add(30);\n\n        int index = 0;\n        int temp = 5 * 5 - 10; // Unrelated computation\n\n        // Conditional logic with misleading paths\n        if (temp == 15) {\n            list.add(40);\n        } else {\n            // More misleading operations\n            list.remove((Integer)20);\n            list.add(50);\n            list.add(60);\n            temp = list.size();\n        }\n\n        // Another unrelated computation\n        int compValue = (int)Math.pow(2, 3) + 7;\n\n        if (compValue > 10) {\n            index = 1;\n        } else {\n            index = 2;\n        }\n\n        // This is the real write operation that affects the read point identified\n        /* write */ list.add(index, 99);\n\n        // Other read-like operations\n        int unrelatedValue = list.get(3); // Does not affect\n\n        // Further manipulations that do not affect final outcome\n        list.add(70);\n        list.remove((Integer)30);\n\n        /* read */ int ret = list.get(1);\n\n        System.out.println(\"Returned value: \" + ret);\n    }\n}\n",
    "Internal Variable": "array[1]"
}