{
    "Code": "import java.util.ArrayList;\n\npublic class Main {\n    public static void main(String[] args) {\n        ArrayList<Integer> list = new ArrayList<>();\n        // Initial fill up of the list\n        for (int i = 0; i < 10; i++) {\n            list.add(i * 2);\n        }\n\n        /* Intermediate computations */\n        int randValue = (int) Math.pow(3, 3);\n        int misleadingIndex = randValue % 4;\n        for (int j = 0; j < 5; j++) {\n            list.add(j + 100);\n        }\n        list.remove(0);\n\n        /* Conditional logic */\n        if (list.size() > 12) {\n            list.set(2, 999);\n            /* write */ list.add(0, 42);\n        } else {\n            list.add(0, 84);\n        }\n\n        /* Other operations that do not affect the read point */\n        misleadingIndex = list.get(2);\n        list.remove(misleadingIndex);\n        list.set(3, 55);\n\n        /* read */ int ret = list.get(0);\n\n        // Output\n        System.out.println(\"Read value: \" + ret);\n    }\n}",
    "Internal Variable": "elementData[0]"
}