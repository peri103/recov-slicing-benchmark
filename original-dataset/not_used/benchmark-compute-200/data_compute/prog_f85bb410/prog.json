{
  "Code": "import java.util.ArrayList;\n\npublic class Main {\n    public static void main(String[] args) {\n        ArrayList<Integer> list = new ArrayList<>();\n\n        // Initializing the list with some values\n        list.add(100);\n        list.add(200);\n        list.add(300);\n\n        // Intermediate unrelated calculations\n        int x = 10;\n        int y = 20;\n        int z = x * y + 50;\n\n        // This section modifies the data structure in obfuscated ways\n        if (z % 2 == 0) {\n            list.add(400);\n        } else {\n            list.add(500);\n        }\n\n        // More unrelated computation\n        int temp = z / 2 - 5;\n\n        // Misleading logic\n        for (int i = 0; i < 3; i++) {\n            list.add(i + 10);\n        }\n\n        // Conditional logic determining flow\n        if (temp > 10) {\n            /* write */ list.set(1, 250); // This is the actual write point affecting the read point\n        }\n\n        // Adding more elements that don't affect the value of interest\n        list.add(600);\n        list.add(700);\n        list.remove(0);\n\n        // A misleading write that doesn't influence the read\n        list.set(0, 1234);\n\n        // Some more data structure manipulation\n        list.add(800);\n        list.add(900);\n        list.remove(list.size() - 1);\n\n        // The read point\n        /* read */ int ret = list.get(1);\n        System.out.println(\"Read value: \" + ret);\n    }\n}",
  "Internal Variable": "elementData[1]"
}