{
    "Code": "import java.util.LinkedList;\n\npublic class Main {\n    public static void main(String[] args) {\n        LinkedList<Integer> list = new LinkedList<>();\n        /* other operations */\n        int rand = (int) (Math.random() * 10);\n        /* other operations */\n        list.add(10); // element at index 0\n        list.add(20); // element at index 1\n        list.add(30); // element at index 2\n        list.add(40); // element at index 3\n        /* other operations */\n        /* write */ if (rand % 2 == 0) {\n            list.set(1, 99); // conditionally modify\n        } else {\n            list.set(2, 77); // misleading modification\n        }\n        /* other operations */\n        list.addFirst(5); // now 5 is at index 0\n        list.removeLast(); // remove element 40\n        list.add(2, 15); // inserting at index 2, shifting others\n        /* other operations */\n        /* read */ int ret = list.get(1); // should read 99 if write is effective\n        System.out.println(ret);\n    }\n}\n",
    "Internal Variable": "elementData[1]"
}