{
  "Code": "import java.util.TreeSet;\n\npublic class Main {\n    public static void main(String[] args) {\n        TreeSet<Integer> set = new TreeSet<>();\n\n        // Adding some initial values to TreeSet\n        set.add(10);\n        set.add(20);\n        set.add(30);\n        set.add(40);\n\n        // Intermediate variables\n        int alpha = 15;\n        int beta = 25;\n\n        // Intermediate logic\n        if (alpha > 10 && beta < 30) {\n            set.add(50);\n            set.remove(20);\n        }\n        set.add(5);\n        set.remove(30);\n\n        // Correct write affecting read point\n        // Obfuscated by unrelated logic and conditions\n        if (alpha < beta) {\n            /* write */ set.add(25);\n        } else {\n            set.add(35);\n        }\n\n        // Misleading modifications to the TreeSet\n        set.add(15);\n        set.remove(10);\n\n        // Calculate current size for obfuscation\n        int setSize = set.size();\n\n        // More unrelated operations\n        System.out.println(\"Intermediate Size of Set: \" + setSize);\n\n        // Calculate target element for reading\n        int nthElement = set.headSet(35).size();\n\n        // Execute a read point\n        /* read */ int ret = (Integer) set.toArray()[nthElement];\n\n        System.out.println(\"Read Value: \" + ret);\n    }\n}",
  "Internal Variable": "elementData[nthElement]"
}