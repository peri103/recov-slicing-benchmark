{
  "Code": "import java.util.Vector;\n\npublic class Main {\n    public static void main(String[] args) {\n        Vector<Integer> vector = new Vector<>();\n\n        // Fill the vector with misleading elements\n        for (int i = 0; i < 10; i++) {\n            vector.add(i * 10);\n        }\n\n        // Some unrelated variable manipulations\n        int a = 10;\n        int b = 20;\n        int c = a * b;\n\n        // Conditional to obfuscate execution path\n        if (c % 2 == 0) {\n            /* write */ vector.add(42); // This element will affect the read\n        } else {\n            vector.add(99); // Misleading addition\n        }\n\n        // Some more intermediate operations\n        for (int j = 0; j < 5; j++) {\n            vector.set(j, vector.get(j) + 3);\n        }\n\n        vector.remove(0); // Misleading remove operation\n        vector.insertElementAt(85, 1); // Misleading insertion\n\n        // Some unrelated assignments\n        int x = c + 100;\n        int y = x - 50;\n\n        // More vector manipulations\n        vector.add(0, 200); // Another insertion at a different point\n\n        // A few unrelated condition checks\n        if (y > 125) {\n            vector.add(500);\n        } else {\n            vector.add(600);\n        }\n\n        // Misleading get operations\n        int dummy = vector.get(3);\n        dummy = vector.get(2);\n\n        // Obfuscated read point\n        /* read */ int ret = vector.get(11); // This retrieves the value 42\n\n        // Output the result to verify\n        System.out.println(\"Read value: \" + ret); // Should print 42\n    }\n}",
  "Internal Variable": "elementData[11]"
}