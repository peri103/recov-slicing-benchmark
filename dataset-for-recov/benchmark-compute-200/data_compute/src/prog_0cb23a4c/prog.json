{
  "Code": "import java.util.Vector;\n\npublic class Main {\n    public static void main(String[] args) {\n        Vector<Integer> vector = new Vector<>();\n        \n        // Filling the vector with initial values\n        for (int i = 0; i < 10; i++) {\n            vector.add(i * i);\n        }\n        \n        // Intermediate computations and misleading operations\n        int x = complexCalculation();\n        if (x % 2 == 0) {\n            vector.add(100);\n        } else {\n            vector.add(200);\n        }\n\n        // Another unrelated add operation\n        vector.add(300);\n        \n        // Conditional logic that does not affect the read point retrieval\n        if (vector.size() > 5) {\n            vector.set(5, vector.get(5) * 2);\n        }\n        \n        // Some dummy processing\n        int dummy = vector.get(3);\n        dummy *= 10;\n        \n        /* write */ vector.add(42);\n\n        // More misleading operations\n        vector.remove(2);\n        vector.set(1, vector.get(1) + 5);\n        vector.add(500);\n        \n        // Final retrieval which relates to the necessary write \n        /* read */ int ret = vector.get(10);\n        System.out.println(\"Retrieved value: \" + ret);\n    }\n    \n    private static int complexCalculation() {\n        // Simulate complex calculation\n        return (int) (Math.random() * 100) + 1;\n    }\n}",
  "Internal Variable": "elementData[10]"
}