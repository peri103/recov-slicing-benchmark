import org.junit.jupiter.api.Test;

class ConvertedTest {
    @Test
    public void testMainLogic() {
        // Block 1:
        int n = 100;
        // Block 2:
        int a = (n % 2 == 0) ? (n / 2) : (n / 2) + 1;
        // Block 3:
        System.out.println(a);
        // Block END.
    }
    }

