import org.junit.jupiter.api.Test;

class _00015544Test {
    @Test
    public void testMainLogic() {
        // Block 1:
        int x = 12, y = 3, z = 1;
        // Block 2:
        x -= 2 * z;
        // Block 3:
        System.out.println(x / (y + z) + x % (y + z) / y);
        // Block END.
    }
    }

