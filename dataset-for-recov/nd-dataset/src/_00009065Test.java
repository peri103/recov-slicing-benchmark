import org.junit.jupiter.api.Test;

class _00009065Test {
    @Test
    public void testMainLogic() {
        // Block 1:
        int A = 15;
        // Block 2:
        int B = 18;
        // Block 3:
        int C = 26;
        // Block 4:
        int D = 35;
        // Block 5:
        int E = 36;
        // Block 6:
        int Q = 18;
        // Block 7:
        int F = A - B;
        // Block 8:
        int G = A - C;
        // Block 9:
        int H = A - D;
        // Block 10:
        int I = A - E;
        // Block 11:
        int J = B - C;
        // Block 12:
        int K = B - D;
        // Block 13:
        int L = B - E;
        // Block 14:
        int M = C - D;
        // Block 15:
        int N = C - E;
        // Block 16:
        int O = D - E;
        // Block 17:
        int P = Math.max(Math.max(Math.max(Math.max(Math.max(Math.max(Math.max(Math.max(Math.max(Math.max(
            Math.abs(F), Math.abs(G)), Math.abs(H)), Math.abs(I)), Math.abs(J)), Math.abs(K)), 
            Math.abs(L)), Math.abs(M)), Math.abs(N)), Math.abs(O)));
        // Block 18:
        if (P <= Q) {
            // Block 19:
            System.out.println("Yay!");
        // Block 20:
        } else {
            // Block 21:
            System.out.println(":(");
        }
        // Block END.
    }
    }

