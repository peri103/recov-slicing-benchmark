import org.junit.jupiter.api.Test;

class _00005464Test {
    @Test
    public void testMainLogic() {
        // Block 1:
        int n = 5;
        // Block 2:
        if (n / 2 == (double) n / 2) {
            // Block 3:
            int m = n / 2;
            // Block 4:
        } else {
            // Block 5:
            int m = n / 2 + 1;
        }
        // Block 6:
        System.out.println(m);
        // Block END.
    }
    }

