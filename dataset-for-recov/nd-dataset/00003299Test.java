import org.junit.jupiter.api.Test;

class _00003299Test {
    @Test
    public void testMainLogic() {
        // Block 1:
        int a = 20, b = 15;
        // Block 2:
        System.out.println((a - (b * 2)) > 0 ? (a - (b * 2)) : 0);
        // Block END.
    }
    }

