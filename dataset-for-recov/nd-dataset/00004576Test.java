import org.junit.jupiter.api.Test;

class ConvertedTest {
    @Test
    public void testMainLogic() {
        // Block 1:
        String arg = "internationalization";
        // Block 2:
        System.out.println(arg.charAt(0) + String.valueOf(arg.length() - 2) + arg.charAt(arg.length() - 1));
        // Block END.
    }
    }
}
