import org.junit.jupiter.api.Test;

class ConvertedTest {
    @Test
    public void testMainLogic() {
        // Block 1:
        char[] a = {'s', 'm', 'i', 'l', 'e', 's'};
        // Block 2:
        System.out.println(a[0] + String.valueOf(a.length - 2) + a[a.length - 1]);
        // Block END.
    }
    }

