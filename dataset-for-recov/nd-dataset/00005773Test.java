import org.junit.jupiter.api.Test;

class ConvertedTest {
    @Test
    public void testMainLogic() {
        // Block 1:
        int n = 5;
        // Block 2:
        System.out.println((n / 2) + (n % 2 != 0 ? 1 : 0));
        // Block END.
    }
    }
}
