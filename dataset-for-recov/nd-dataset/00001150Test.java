import org.junit.jupiter.api.Test;

class _00001150Test {
    @Test
    public void testMainLogic() {
        // Block 1:
        int N = 112;
        // Block 2:
        String S = String.valueOf(N);
        // Block 3:
        if (S.charAt(0) == S.charAt(1) && S.charAt(1) == S.charAt(2)) {
            // Block 4:
            System.out.println(N);
        } else {
            // Block 5:
            // Block 6:
            System.out.println(111 * (N / 111 + 1));
        }
        // Block END.
    }
    }

