import org.junit.jupiter.api.Test;

class ConvertedTest {
    @Test
    public void testMainLogic() {
        // Block 1:
        int n = 3, k = 3;
        // Block 2:
        int[] a = {1, 2, 3};
        // Block 3:
        System.out.println((n - 1) / (k - 1) + ((n - 1) % (k - 1) > 0 ? 1 : 0));
        // Block END.
    }
    }
}
